#include "estruturas.h"

// Aqui você cria e inicializa as variáveis de verdade
infoTela tela = { 1200, 800, "Hollow Knight" };

infoPersonagem personagem = { { 10, 700 }, { 0 } }; // {0} limpa a struct da textura

infoMapa map = { 151, 16, "Mapas/MapaTeste.txt", NULL };

infoBloco bloco = { 10.0f, 10.0f };